name: Build and Push Docker Image

on:
  push:
    branches:
      - main

permissions:
  contents: read
  packages: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GHCR_PAT }}

      - name: Convert repository owner name to lowercase
        id: vars
        run: echo "owner=$(echo ${{ github.repository_owner }} | awk '{print tolower($0)}')" >> $GITHUB_ENV

      - name: Build and push Docker image for main branch
        if: github.ref == 'refs/heads/main'
        uses: docker/build-push-action@v2
        with:
          context: ./src/polyscope-main
          file: ./src/polyscope-main/Dockerfile
          push: true
          tags: ghcr.io/${{ env.owner }}/polyscope:latest

      - name: Build and push Docker image for release branch
        if: github.ref == 'refs/heads/release/v1.0.1'
        uses: docker/build-push-action@v2
        with:
          context: ./src/polyscope-main
          file: ./src/polyscope-main/Dockerfile
          push: true
          tags: ghcr.io/${{ env.owner }}/polyscope-development-test:latest

      - name: Post Build Cleanup
        run: docker system prune -f
